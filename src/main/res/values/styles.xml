<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.NoActionBar.Fullscreen">

        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="Dialog_Fullscreen">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <style name="Dialog_Help_Fullscreen">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <!--<item name="android:windowBackground">@android:color/transparent</item>-->
    </style>

    <!--                  -->
    <style name="text_face_author">
        <item name="android:textSize">@dimen/textsize_20</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="CustomCheckBoxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_style</item>
    </style>

    <!-- 无黑色背景的dialog -->
    <style name="NewSettingDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@drawable/dialog_border_bg</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 是否是黑色背景 -->
        <!-- 模糊 -->
    </style>

    <style name="text_nomal">
        <item name="android:textSize">@dimen/textsize_20</item>
        <item name="android:textColor">@color/hint_diakog_cancle_textcoklor</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="AdvancedEditFragmentStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:paddingBottom">@dimen/fragment_padding_top_bottom</item>
        <item name="android:paddingLeft">@dimen/fragment_padding_left_right</item>
        <item name="android:paddingRight">@dimen/fragment_padding_left_right</item>
        <item name="android:paddingTop">@dimen/fragment_padding_top_bottom</item>

    </style>

    <style name="edit_style" >

        <item name="android:minHeight">22dp</item>
        <item name="android:minWidth">70dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:gravity">center</item>
        <!--<item name="android:textStyle">bold</item>-->
        <item name="android:textColor">#3d3d3d</item>
        <item name="android:background">@color/white</item>
        <item name="android:inputType">number</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:text">100</item>

    </style>

    <style name="no_Multi_Touch">
        <item name="android:splitMotionEvents">false</item>
        <item name="android:windowEnableSplitTouch">false</item>
    </style>

    <style name="btn_size">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
